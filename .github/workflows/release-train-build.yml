name: "Release Train – Build"
run-name: "${{ inputs.callback-ref }} – Build"
"on":
  workflow_dispatch:
    inputs:
      callback:
        description: "Repository to which a callback should be made upon completion"
        required: true
        type: "string"
      callback-ref:
        description: "Ref in the callback repository to which a callback should be made upon completion"
        required: true
        type: "string"
      release-train-maven-repository-url:
        description: "URL of a Maven repository to be used to resolve artifacts of projects earlier in the train"
        required: true
        type: "string"
permissions:
  contents: "read"
concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
jobs:
  build-release:
    name: "Build Release"
    runs-on: "ubuntu-latest"
    steps:
    - name: "Set up Java"
      id: "set-up-java"
      uses: "actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654" # 5.2.0
      with:
        distribution: "liberica"
        java-version: "17"
    - name: "Check Out Code"
      id: "check-out-code"
      uses: "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" # 6.0.2
      with:
        ref: "${{ github.ref }}"
    - name: "Build Release"
      id: "build-release"
      uses: "./.github/actions/release-train-build"
      env:
        RELEASE_TRAIN_MAVEN_REPOSITORY_PASSWORD: "${{ secrets.PACKAGES_RELEASE_TOKEN }}"
        RELEASE_TRAIN_MAVEN_REPOSITORY_URL: "${{ inputs.release-train-maven-repository-url }}"
        RELEASE_TRAIN_MAVEN_REPOSITORY_USERNAME: "${{ secrets.PACKAGES_RELEASE_USERNAME }}"
    - name: "Upload Deployment Repository"
      id: "upload-deployment-repository"
      uses: "actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a" # 7.0.1
      with:
        name: "deployment-repository"
        path: "deployment-repository/**"
    - name: "Save Build System Caches"
      id: "save-build-system-caches"
      uses: "actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae" # 5.0.5
      with:
        key: "release-train-${{ inputs.callback-ref }}-${{ github.ref_name }}"
        path: "~/.m2/repository"
    - name: "Send Callback"
      id: "send-callback"
      if: "${{ !cancelled() }}"
      env:
        GH_TOKEN: "${{ secrets.ACTIONS_RELEASE_TOKEN }}"
      run: "gh workflow run callback --repo ${{ inputs.callback }} --ref ${{ inputs.callback-ref }} --field deployment-repository-artifact-identifier=${{ steps.upload-deployment-repository.outputs.artifact-id }} --field release-branch=${{ github.ref_name }} --field release-repository=${{ github.repository }} --field result=${{ job.status == 'success' && 'built' || 'build-failed' }}"
